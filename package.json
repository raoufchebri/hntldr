{
  "name": "hntldr",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "deploy": "bash scripts/deploy-all.sh",
    "deploy:tracker": "cd packages/hn-tracker && ./deploy.sh",
    "deploy:summarizer": "cd packages/story-summarizer && ./deploy.sh",
    "deploy:newsletter": "cd packages/newsletter && ./deploy.sh"
  },
  "devDependencies": {
    "eslint": "^8.57.0",
    "prettier": "^3.2.5"
  },
  "engines": {
    "node": ">=20.0.0"
  }
} 